# Nanobot WebUI - Python Dependencies (v3.2 - LlamaParse + Local Embedding)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Validation
pydantic>=2.12.0

# HTTP Client
httpx>=0.27.0
asyncpg>=0.29.0
aiohttp>=3.9.0  # Required for gateway streaming

# File Uploads
python-multipart>=0.0.9
aiofiles>=23.2.1

# 🌟 LlamaParse Cloud Parser（替代 OpenDataLoader）
# 支持 130+ 格式 + 本地文件上传 + Agentic OCR
llama_cloud>=2.3.0

# 🌟 本地 Embedding 模型（替代 OpenAI API）
# Stage 7 向量生成使用本地模型，不依賴外部 API
sentence-transformers>=2.2.0

# Logging
loguru>=0.7.0

# 🌟 vanna 移至獨立的 vanna-service 容器
# WebUI 透過 HTTP API 調用 vanna-service:8082
# 無需本地安裝 vanna 依賴