# Nanobot WebUI - Python Dependencies (v3.2 - LlamaParse + PyMuPDF + Local Embedding)

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Validation
pydantic>=2.12.0

# HTTP Client
httpx>=0.27.0
asyncpg>=0.29.0
aiohttp>=3.9.0  # Required for gateway streaming

# File Uploads
python-multipart>=0.0.9
aiofiles>=23.2.1

# 🌟 LlamaParse Cloud Parser（替代 OpenDataLoader）
# 支持 130+ 格式 + 本地文件上传 + Agentic OCR
llama_cloud>=2.3.0

# 🌟 PyMuPDF（Stage 0 截取封面）
# pip install pymupdf，但 import fitz
# 用于独立运行 Stage 0，不等待 LlamaParse
PyMuPDF>=1.24.0

# 🌟 本地 Embedding 模型（替代 OpenAI API）
# Stage 7 向量生成使用本地模型，不依賴外部 API
sentence-transformers>=2.2.0

# Logging
loguru>=0.7.0