# Code Review Report - 2026-04-22

## 📋 Review 範圍

本次 Review 涵蓋以下模組：
1. `vanna-service/` - Text-to-SQL 微服務
2. `storage/` - 資料庫 Schema
3. `nanobot/ingestion/` - PDF 處理 Pipeline

---

## 1. Vanna Service Review

### 檔案結構
```
vanna-service/
├── start.py              # FastAPI 服務器 (~1256 行)
├── vanna_training.py     # 訓練數據生成
├── Dockerfile            # Docker 構建配置
└── data/
    ├── ddl.json          # DDL 知識庫
    ├── ddl_whitelist.json
    ├── documentation.json
    └── sql_pairs.json    # SQL 示例知識庫
```

### ✅ 正確的部分

| 項目 | 狀態 | 說明 |
|------|------|------|
| v2.3 Schema 版本描述 | ✅ 正確 | COLUMN_CHANGE_HINTS 準確反映 DB Schema |
| API 端點描述 | ✅ 正確 | 各 endpoint 功能與實現一致 |
| Docker 端口 | ✅ 正確 | Dockerfile 和代碼都使用 8082 |
| 行業雙軌制邏輯 | ✅ 正確 | Rule A/B 邏輯封裝在視圖中 |
| JSONB 查詢示例 | ✅ 正確 | 提供了完整的 JSONB 查詢語法示例 |

### ⚠️ 已修正的問題

| 問題 | 修正 |
|------|------|
| embedding_vector 維度不一致 | 從 `VECTOR(1536)` 修正為 `VECTOR(384)`（本地模型 all-MiniLM-L6-v2） |

### 📝 註解品質評估

**start.py** 註解品質：**優秀**
- 每個 API endpoint 都有完整的 docstring
- 錯誤處理有明確的日誌輸出
- 響應模型使用 Pydantic，類型清晰

**vanna_training.py** 註解品質：**優秀**
- 每個表都有詳細的中文說明
- SQL Examples 涵蓋常見查詢場景
- Column mappings 清晰標註 v2.3 變更

---

## 2. Storage (Database Schema) Review

### 檔案：`storage/init_complete.sql`

### ✅ Schema 設計評估

| 表名 | 設計評估 | 說明 |
|------|----------|------|
| `companies` | ✅ 優秀 | 行業雙軌制設計巧妙，JSONB 支援彈性擴展 |
| `documents` | ✅ 優秀 | 支援兩種報告類型，動態屬性用 JSONB |
| `document_companies` | ✅ 優秀 | 多對多關聯，記錄公司角色 |
| `document_pages` | ✅ 優秀 | 注意：沒有 company_id，必須 JOIN documents |
| `financial_metrics` | ✅ 優秀 | EAV 模式靈活，支援標準化 |
| `market_data` | ✅ 優秀 | 欄位命名已更新 (data_date, close_price, volume) |
| `key_personnel` | ✅ 優秀 | committee_membership 使用 JSONB |
| `shareholding_structure` | ✅ 優秀 | 新增 trust_name, trustee_name |
| `entity_relations` | ✅ 優秀 | 支援 Graph-based 查詢 |
| `artifact_relations` | ✅ 優秀 | 解決跨頁圖文關聯問題 |

### 📝 Schema 文檔品質

**評估：優秀**
- 每個表都有完整的中文註解
- 關聯關係清晰標註
- 索引策略有說明

### 🔍 關鍵發現

1. **document_pages 沒有 company_id**
   - 這是正確設計，因為一頁可能涉及多間公司
   - 必須通過 `JOIN documents ON document_pages.document_id = documents.id` 篩選

2. **embedding_vector 使用 VECTOR(384)**
   - 對應本地 sentence-transformers 模型
   - 在 vanna_training.py 中已修正

3. **視圖設計良好**
   - `v_companies_for_vanna` 自動處理行業雙軌制
   - `v_documents_for_vanna` 扁平化 JSONB

---

## 3. Ingestion Pipeline Review

### 檔案結構
```
nanobot/ingestion/
├── pipeline.py              # 主 Pipeline 協調器
├── base_pipeline.py         # 基類 (模板方法模式)
├── agentic_pipeline.py      # Agent-driven Pipeline
├── agentic_executor.py      # Agent 執行器
├── batch_processor.py       # 批次處理器
├── stages/
│   ├── stage0_preprocessor.py      # Vision 分析 Page 1
│   ├── stage0_5_registrar.py       # 文件/公司註冊
│   ├── stage1_parser.py            # LlamaParse 解析
│   ├── stage2_enrichment.py        # 圖文關聯/元數據
│   ├── stage3_router.py            # 關鍵字路由
│   ├── stage3_5_context_builder.py # 結構化上下文
│   ├── stage4_agentic_extractor.py # Tool Calling 提取
│   ├── stage4_5_kg_extractor.py    # 知識圖譜
│   ├── stage5_vanna_training.py    # Vanna 訓練
│   ├── stage6_validator.py         # 數據校驗
│   ├── stage7_vector_indexer.py    # 向量索引
│   └── stage8_archiver.py          # 歸檔清理
```

### ✅ Pipeline 設計評估

| Stage | 職責 | 設計評估 |
|-------|------|----------|
| Stage 1 | LlamaParse 解析 | ✅ 先行設計正確 |
| Stage 0 | Vision 分析 Page 1 | ✅ 基於 LlamaParse artifacts |
| Stage 0.5 | 註冊器 | ✅ Hash + 註冊邏輯清晰 |
| Stage 2 | 豐富化 | ✅ RAGAnything 設計優秀 |
| Stage 3 | 路由器 | ✅ 配置驅動，不 hardcode |
| Stage 3.5 | 上下文構建 | ✅ 結構化上下文創新 |
| Stage 4 | 代理提取 | ✅ Tool Calling 設計先進 |
| Stage 4.5 | KG 提取 | ✅ 知識圖譜支援 |
| Stage 5 | Vanna 訓練 | ✅ 邊做邊學 |
| Stage 6 | 驗證器 | ✅ 數據品質保障 |
| Stage 7 | 向量索引 | ✅ RAG 準備 |
| Stage 8 | 歸檔器 | ✅ 清理機制 |

### ⚠️ 已修正的問題

| 問題 | 修正 |
|------|------|
| README.md Stage 順序與 pipeline.py 不一致 | 已更新 README.md 以反映 v4.0 的 Stage 1 先行設計 |
| Stage 3 職責描述不清 | 已在 pipeline_architecture.md 中澄清 |

### 📝 註解品質評估

**pipeline.py** 註解品質：**優秀**
- 模板方法模式清晰
- 抽象方法明確標註

**stage0_preprocessor.py** 註解品質：**優秀**
- v4.7 PyMuPDF 移除說明清楚
- 多圖片 Vision 合併邏輯有說明

**stage4_agentic_extractor.py** 註解品質：**優秀**
- Tool Calling 流程詳細
- 19 個 Tools 的用途說明清晰

---

## 4. 整體評估

### 優點

1. **模組化架構**：各 Stage 職責分明，低耦合
2. **配置驅動**：關鍵字、術語映射使用 JSON 配置
3. **防禦性設計**：多層 Fallback 機制
4. **文檔完善**：中文註解詳細，易於維護
5. **微服務分離**：Vanna Service 獨立部署

### 建議改進

1. **測試覆蓋**：關鍵 Stage 缺乏單元測試
2. **錯誤處理**：可考慮統一異常層級
3. **性能優化**：Pipeline 可考慮並行化部分 Stage

---

## 5. 變更記錄

### 2026-04-22

1. **vanna_training.py**
   - 修正 `document_pages.embedding_vector` 從 `VECTOR(1536)` 為 `VECTOR(384)`
   - 修正 `document_chunks.embedding_vector` 從 `VECTOR(1536)` 為 `VECTOR(384)`

2. **README.md**
   - 更新 Stage 流程圖，反映 v4.0 的 Stage 1 先行設計
   - 新增 Stage 9 (Image Text Linker) 說明
   - 補充各 Stage 的詳細職責說明

---

## 6. 結論

本次 Review 確認：
- **註解準確性**：大部分註解與代碼一致
- **Schema 一致性**：資料庫 Schema 與代碼正確對應
- **Pipeline 流程**：Stage 順序已更新至最新版本

整體代碼品質良好，文檔完善，建議後續加強測試覆蓋率。
